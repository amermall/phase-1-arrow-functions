// has a function expression called divide
const divide = function() {
    return (2000 / 100);
}

// has an arrow function called square
const square = (num1) => num1 * num1;

// 1) has an arrow function called add...
// 2) add arrow function takes two parameters and adds them together
const add = (number1, number2) => number1 + number2;
